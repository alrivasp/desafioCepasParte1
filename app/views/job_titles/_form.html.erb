<%= form_with(model: job_title, local: true) do |form| %>
  <% if job_title.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job_title.errors.count, "error") %> prohibited this job_title from being saved:</h2>

      <ul>
      <% job_title.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, "Nombre" %>
    <%= form.text_field :name %>
  </div>

   <div class="container actions contenedor-cartel2 ">
    <div class="container btn cartel2 p-4 ">
      <%= form.submit class: 'text-decoration-none text-white skip-style' %>
    </div>
  </div>
<% end %>
